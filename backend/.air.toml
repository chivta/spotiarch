tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/api ./cmd/api"
  bin = "./tmp/api"
  entrypoint = ["./tmp/api"]
  exclude_dir = ["tmp"]
  include_ext = ["go"]
  pre_cmd = ["mkdir -p tmp"]
  stop_on_error = true

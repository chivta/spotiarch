apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: spotiarch
  labels:
    app: api
spec:
  selector:
    app: api
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
  type: ClusterIP

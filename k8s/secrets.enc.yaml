# Placeholder only. Before deployment, replace this List with SOPS-encrypted
# app-secrets and ghcr-secret Secret resources. app-secrets must provide:
# DATABASE_URL, DATABASE_PASSWORD, JWT_SECRET, SPOTIFY_CLIENT_ID,
# SPOTIFY_CLIENT_SECRET, and SPOTIFY_REFRESH_TOKEN. Never commit plaintext values.
apiVersion: v1
kind: List
items: []

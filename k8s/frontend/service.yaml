apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: spotiarch
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
